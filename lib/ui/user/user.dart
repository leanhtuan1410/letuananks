import 'package:flutter/material.dart';

class accoutnTab extends StatelessWidget {
  const accoutnTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Account')),
    );
  }
}
